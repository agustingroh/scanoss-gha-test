
name: Example SCANOSS Action

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  checks: write
  actions: read

jobs:
  scanoss-analysis:
    name: SCANOSS Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        id: checkout
        uses: actions/checkout@v4

      - name: Run SCANOSS analysis
        id: scan
        uses: scanoss/actions-scan@v0.3.3
        with:
          policies: copyleft, undeclared
          dependencies.enabled: true
          licenses.copyleft.include: MIT,Apache-2.0,BSD-3-Clause,ISC,Python-2.0,CC0-1.0,0BSD
